from setuptools import setup, find_packages
setup(
    name = 'GenAI Info Retrieval Project',
    version = "0.0.0",
    auther = "vid",
    auther_email = "vidvata@gmail.com",
    package = find_packages(),
    install_requires = []
)